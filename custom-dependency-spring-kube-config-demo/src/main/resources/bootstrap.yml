spring:
  application:
    name: custom-dependency-spring-kube-config-demo
  cloud:
    config:
      enabled: true
    kubernetes:
      enabled: true
      config:
        name: custom-dependency-spring-kube-config-demo
        namespace: default
      reload:
        enabled: false
        strategy: restart_context